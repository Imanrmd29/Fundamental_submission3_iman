<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iman.fundamental_submission3_iman">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fundamental_submission3_iman.">
        <activity
            android:name=".themeActivity.ThemeActivity"
            android:exported="true" />
        <activity
            android:name=".FavoriteActivity.FavoriteActivity"
            android:exported="true" />
        <activity
            android:name=".splashScreen.SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".detailActivity.DetailUserActivity"
            android:exported="true" />
        <activity
            android:name=".mainActivity.MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>